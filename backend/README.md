# Real-time Transcription with faster-whisper

This directory contains a Python script for real-time audio transcription using the `faster-whisper` library.

## Requirements

### Python

*   Python 3.8 or newer

### Python Packages

The required Python packages are listed in `requirements.txt`. You can install them using pip:

```bash
pip install -r requirements.txt
```

